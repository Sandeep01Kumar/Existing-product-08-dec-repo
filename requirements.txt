Flask==3.1.3
# gunicorn: UNIX-only production WSGI server (runs `gunicorn wsgi:app`)
gunicorn==26.0.0
# waitress: pure-Python, cross-platform production WSGI server (alternative to gunicorn)
waitress==3.0.2
